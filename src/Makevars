ifeq ($(OS),Darwin)
PKG_CFLAGS += -Xpreprocessor -fopenmp -I/opt/homebrew/opt/libomp/include
PKG_LIBS += -lomp -L/opt/homebrew/opt/libomp/lib
else
PKG_CFLAGS += $(SHLIB_OPENMP_CFLAGS)
PKG_LIBS += $(SHLIB_OPENMP_CFLAGS)
endif